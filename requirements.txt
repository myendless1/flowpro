# Python 3.10 training and Wan-VA inference environment.
# Do not install this file into the Python 3.8 Astribot/ROS environment;
# use requirements-robot.txt there instead.
numpy==1.26.4
torch==2.9.0
torchvision==0.24.0
torchaudio==2.9.0
diffusers==0.36.0
transformers==4.55.2
accelerate
deepspeed
einops
easydict
# Install separately with --no-build-isolation after torch is available.
tqdm
imageio[ffmpeg]
websockets
msgpack
opencv-python
matplotlib
ftfy
safetensors
Pillow
h5py>=3.9
requests>=2.31
# Install separately with --no-deps after this file. LeRobot 0.3.3 declares
# torchvision<0.23, while Wan-VA requires the Torch 2.9 / torchvision 0.24 ABI.
scipy
wandb
pytest>=7
